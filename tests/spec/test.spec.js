describe("Test :", function() {
  
  var a = 1;
  var b = 2;

  it("a devrait être égal à 1", function() {
    expect(a).toEqual(1);
  });

  it("b devrait être égal à 1", function() {
    expect(b).toEqual(1);
  });

});